apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: org2-peer0
  namespace: org2-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: org2-peer0
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-devlope
        namespace: flux-devlope
      chart: platforms/hyperledger-fabric/charts/peernode    
  values:
    metadata:
      namespace: org2-net
      images:
        couchdb: couchdb:3.1
        peer: hyperledger/fabric-peer:2.2.2
        alpineutils: index.docker.io/hyperledgerlabs/alpine-utils:1.0
        
    peer:
      name: peer0
      gossippeeraddress: peer0.org2-net:7051
      gossipexternalendpoint: peer0.org2-net:7051
      localmspid: org2MSP
      loglevel: info
      tlsstatus: true
      builder: hyperledger/fabric-ccenv:2.2.2
      couchdb:
        username: org2-user

    storage:
      peer:
        storageclassname: org2sc
        storagesize: 512Mi
      couchdb:
        storageclassname: org2sc
        storagesize: 1Gi

    vault:
      role: vault-role
      address: http://54.185.72.226:8200
      authpath: devlopeorg2-net-auth
      secretprefix: secretsv2/data/crypto/peerOrganizations/org2-net/peers/peer0.org2-net
      secretambassador: secretsv2/data/crypto/peerOrganizations/org2-net/ambassador
      serviceaccountname: vault-auth
      imagesecretname: regcred
      secretcouchdbpass: secretsv2/data/credentials/org2-net/couchdb/org2?user

    service:
      servicetype: ClusterIP
      ports:
        grpc:
          clusteripport: 7051
        events:
          clusteripport: 7053
        couchdb:
          clusteripport: 5984
          
    proxy:
      provider: "none"
      external_url_suffix: none

    config:
      pod:
        resources:
          limits:
            memory: 512M
            cpu: 1
          requests:
            memory: 512M
            cpu: 0.5
